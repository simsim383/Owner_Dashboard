import { useState, useMemo, useCallback, useRef, useEffect } from "react";
import { BarChart, Bar, AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Cell } from "recharts";
import * as XLSX from "xlsx";

// ═══════════════════════════════════════════════════════════════════
// SHOPMATE SALES DASHBOARD v2 — Full Feature Set
// ═══════════════════════════════════════════════════════════════════

const SUPABASE_URL = "https://zqipwxhhqyzzqowlwlfq.supabase.co";
const SUPABASE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpxaXB3eGhocXl6enFvd2x3bGZxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzMzNTA3MjUsImV4cCI6MjA4ODkyNjcyNX0.nIG-rd11_Jc2wGTB4uYpQjJi5l9n9uDcsUdcPOJdh_o";
const SB_HDR={"apikey":SUPABASE_KEY,"Authorization":`Bearer ${SUPABASE_KEY}`,"Content-Type":"application/json","Prefer":"return=representation"};
// Claude API — requires your own API key for standalone deployment
const ANTHROPIC_KEY = "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"; // ← paste your Anthropic API key here
const AI_HDR = ANTHROPIC_KEY ? {"Content-Type":"application/json","x-api-key":ANTHROPIC_KEY,"anthropic-dangerous-direct-browser-access":"true","anthropic-version":"2023-06-01"} : {"Content-Type":"application/json","anthropic-dangerous-direct-browser-access":"true"};
async function sbGet(t,p=""){const r=await fetch(`${SUPABASE_URL}/rest/v1/${t}?${p}`,{headers:SB_HDR});if(!r.ok){const e=await r.json().catch(()=>({}));throw new Error(e?.message||`GET ${t} failed`);}return r.json();}
async function sbPost(t,b){const r=await fetch(`${SUPABASE_URL}/rest/v1/${t}`,{method:"POST",headers:SB_HDR,body:JSON.stringify(b)});if(!r.ok){const e=await r.json().catch(()=>({}));throw new Error(e?.message||`POST ${t} failed`);}return r.json();}
async function sbDelete(t,p){const r=await fetch(`${SUPABASE_URL}/rest/v1/${t}?${p}`,{method:"DELETE",headers:SB_HDR});if(!r.ok){const e=await r.json().catch(()=>({}));throw new Error(e?.message||`DELETE ${t} failed`);}return r.json();}
function getOwnerIdFromURL(){return new URLSearchParams(window.location.search).get("owner")||null;}
// Push parsed ShopMate data to Supabase
async function pushToSupabase(clientId,data){
  const date=data.dates?.start;if(!date||!clientId)return{ok:false,error:"Missing date or client ID"};
  // Check for existing upload
  try{const existing=await sbGet("uploads",`client_id=eq.${encodeURIComponent(clientId)}&report_date=eq.${date}`);
  if(existing.length>0){
    // Delete existing upload + cascade sales
    await sbDelete("daily_sales",`upload_id=eq.${existing[0].id}`);
    await sbDelete("uploads",`id=eq.${existing[0].id}`);
  }}catch(e){console.warn("Duplicate check:",e);}
  // Insert upload record
  const totalGross=data.items.reduce((s,i)=>s+i.gross,0);
  const totalQty=data.items.reduce((s,i)=>s+i.qty,0);
  try{
    const[upload]=await sbPost("uploads",[{client_id:clientId,report_date:date,report_start:data.dates.start,report_end:data.dates.end,filename:data.filename||"upload.xls",row_count:data.items.length,total_gross:totalGross,total_qty:totalQty}]);
    console.log("Upload record created:",upload.id);
    // Insert sales rows in batches of 100
    const rows=data.items.map(i=>({client_id:clientId,upload_id:upload.id,report_date:date,barcode:i.barcode,product:i.product,category:i.category,qty:i.qty,gross:i.gross,net:i.net,gross_profit:i.grossProfit,gross_margin:i.grossMargin,has_cost_data:i.hasCost}));
    for(let i=0;i<rows.length;i+=100){await sbPost("daily_sales",rows.slice(i,i+100));console.log(`Batch ${Math.floor(i/100)+1} inserted`);}
    return{ok:true,uploadId:upload.id,rows:rows.length};
  }catch(e){console.error("Supabase push failed:",e);return{ok:false,error:e.message};}
}
// Load all uploads for a client from Supabase
async function loadFromSupabase(clientId){
  if(!clientId)return[];
  try{
    const uploads=await sbGet("uploads",`client_id=eq.${encodeURIComponent(clientId)}&order=report_date.desc&limit=90`);
    const allDays=[];
    for(const u of uploads){
      const sales=await sbGet("daily_sales",`upload_id=eq.${u.id}&order=gross.desc`);
      allDays.push({items:sales.map(s=>({barcode:s.barcode,product:s.product,category:s.category,qty:s.qty,gross:Number(s.gross),net:Number(s.net),grossProfit:s.gross_profit!=null?Number(s.gross_profit):null,grossMargin:s.gross_margin!=null?Number(s.gross_margin):null,hasCost:s.has_cost_data})),dates:{start:u.report_date,end:u.report_end||u.report_date}});
    }
    return allDays.reverse(); // oldest first
  }catch(e){console.error("Load from Supabase:",e);return[];}
}
// Fetch or create client by owner ID (owner ID is used as the name, UUID is auto-generated)
async function getOrCreateClient(ownerId){
  if(!ownerId)return null;
  try{
    const existing=await sbGet("clients",`name=eq.${encodeURIComponent(ownerId)}`);
    if(existing.length>0)return existing[0];
    // Auto-create — let Supabase generate the UUID
    const[created]=await sbPost("clients",[{name:ownerId,owner_name:ownerId}]);
    console.log("Created client:",created);
    return created;
  }catch(e){
    console.error("Client lookup/create failed:",e);
    return null;
  }
}

const C={bg:"#0B1120",surface:"#111827",card:"#1A2332",cardHover:"#1E293B",accent:"#2E5090",accentLight:"#3B6FD4",accentGlow:"rgba(59,111,212,0.15)",green:"#22C55E",greenDim:"rgba(34,197,94,0.12)",greenText:"#4ADE80",red:"#EF4444",redDim:"rgba(239,68,68,0.12)",redText:"#F87171",orange:"#F59E0B",orangeDim:"rgba(245,158,11,0.12)",orangeText:"#FBBF24",blue:"#3B82F6",blueDim:"rgba(59,130,246,0.12)",white:"#F8FAFC",textPrimary:"#E2E8F0",textSecondary:"#94A3B8",textMuted:"#64748B",border:"rgba(148,163,184,0.08)",divider:"rgba(148,163,184,0.06)",gold:"#D4A843"};
const f=(n)=>`£${Number(n).toLocaleString("en-GB",{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const fi=(n)=>`£${Math.round(Number(n)).toLocaleString("en-GB")}`;
const pct=(n)=>`${Number(n).toFixed(1)}%`;
const dt=(a,b)=>b?((a-b)/b*100).toFixed(1):"0.0";

const Badge=({type,children})=>{const colors={HIGH:{bg:C.greenDim,text:C.greenText,border:"rgba(34,197,94,0.3)"},MED:{bg:C.orangeDim,text:C.orangeText,border:"rgba(245,158,11,0.3)"},LOW:{bg:C.redDim,text:C.redText,border:"rgba(239,68,68,0.3)"},TRACKED:{bg:C.greenDim,text:C.greenText,border:"rgba(34,197,94,0.3)"},UNTRACKED:{bg:C.redDim,text:C.redText,border:"rgba(239,68,68,0.3)"},ALERT:{bg:C.orangeDim,text:C.orangeText,border:"rgba(245,158,11,0.3)"},OK:{bg:C.accentGlow,text:C.accentLight,border:"rgba(59,111,212,0.3)"}};const c=colors[type]||{bg:C.accentGlow,text:C.textSecondary,border:C.border};return<span style={{display:"inline-block",padding:"2px 8px",borderRadius:6,fontSize:10,fontWeight:700,letterSpacing:.5,background:c.bg,color:c.text,border:`1px solid ${c.border}`,textTransform:"uppercase",whiteSpace:"nowrap"}}>{children}</span>};
const Stat=({label,value,sub,trend,small})=>(<div style={{flex:1,minWidth:small?70:100,textAlign:"center"}}><div style={{fontSize:10,color:C.textMuted,textTransform:"uppercase",letterSpacing:1,marginBottom:4,fontWeight:600}}>{label}</div><div style={{fontSize:small?18:22,fontWeight:800,color:C.white,letterSpacing:-.5}}>{value}</div>{sub&&<div style={{fontSize:10,marginTop:2,fontWeight:600,color:trend>0?C.greenText:trend<0?C.redText:C.textMuted}}>{trend>0?"▲":trend<0?"▼":""} {sub}</div>}</div>);
const SectionCard=({title,icon,children,accent,noPad})=>(<div style={{background:C.card,borderRadius:16,marginBottom:16,border:`1px solid ${C.border}`,overflow:"hidden",boxShadow:"0 4px 24px rgba(0,0,0,0.2)"}}><div style={{padding:"14px 16px",display:"flex",alignItems:"center",gap:10,borderBottom:`1px solid ${C.divider}`,background:accent?`linear-gradient(135deg,${C.card},${accent})`:C.card}}><span style={{fontSize:18}}>{icon}</span><span style={{fontSize:14,fontWeight:700,color:C.white,letterSpacing:.3}}>{title}</span></div><div style={{padding:noPad?0:"12px 16px"}}>{children}</div></div>);
const TableRow=({cells,header,highlight})=>(<div style={{display:"flex",alignItems:"center",padding:"8px 16px",gap:6,background:header?"rgba(46,80,144,0.08)":highlight?"rgba(34,197,94,0.04)":"transparent",borderBottom:`1px solid ${C.divider}`}}>{cells.map((cell,i)=><div key={i} style={{flex:cell.flex||1,fontSize:header?9:11,fontWeight:header?700:cell.bold?700:400,color:header?C.textMuted:cell.color||C.textPrimary,textAlign:cell.align||"left",letterSpacing:header?.8:0,textTransform:header?"uppercase":"none",overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{cell.v}</div>)}</div>);
const EmptyState=({msg})=><div style={{textAlign:"center",padding:24,color:C.textMuted,fontSize:12}}>{msg}</div>;
const ChartTip=({active,payload,label})=>{if(!active||!payload?.length)return null;return<div style={{background:C.surface,border:`1px solid ${C.border}`,borderRadius:10,padding:"8px 12px",fontSize:11}}><div style={{color:C.white,marginBottom:4,fontWeight:600}}>{label}</div>{payload.map((p,i)=><div key={i} style={{color:p.color||C.accentLight,fontWeight:700}}>{p.name}: {f(p.value)}</div>)}</div>};

function parseMoney(v){if(v==null||v==="-"||v==="")return null;const n=parseFloat(String(v).replace(/[£,\s]/g,""));return isNaN(n)?null:n}
function parsePct(v){if(v==null||v==="-"||v==="")return null;const n=parseFloat(String(v).replace(/[%,\s]/g,""));return isNaN(n)?null:n}
function extractDates(fn){const b=fn.replace(/^.*[\\/]/,"").replace(/_Item_Sales_Report\.xls[x]?$/i,"");const p=b.split("-");if(p.length<2)return null;try{const d=s=>{const[y,m,dd]=s.split("_");return`20${y}-${m}-${dd}`};return{start:d(p[0]),end:d(p[1])}}catch{return null}}
function parseFile(buf,fn){const wb=XLSX.read(buf,{type:"array"});const sn=wb.SheetNames.find(n=>n.toLowerCase()==="ag-grid")||wb.SheetNames[0];const rows=XLSX.utils.sheet_to_json(wb.Sheets[sn],{header:1});if(rows.length<2)throw new Error("No data");const items=[];for(let i=1;i<rows.length;i++){const r=rows[i];if(!r||!r[0]||!r[1])continue;const gp=parseMoney(r[6]);items.push({barcode:String(r[0]).trim(),product:String(r[1]).trim(),category:String(r[2]||"").trim(),qty:parseInt(r[3])||0,gross:parseMoney(r[4])||0,net:parseMoney(r[5])||0,grossProfit:gp,grossMargin:parsePct(r[7]),hasCost:gp!==null})}return{items,dates:extractDates(fn)}}

function analyzeData(allDays,current){const items=current.items;const tracked=items.filter(i=>i.hasCost);const untracked=items.filter(i=>!i.hasCost);const trackedProfit=tracked.reduce((s,i)=>s+(i.grossProfit||0),0);const trackedNet=tracked.reduce((s,i)=>s+i.net,0);const trackedMargin=trackedNet>0?(trackedProfit/trackedNet)*100:0;const totalGross=items.reduce((s,i)=>s+i.gross,0);const totalNet=items.reduce((s,i)=>s+i.net,0);const totalQty=items.reduce((s,i)=>s+i.qty,0);const untrackedRev=untracked.reduce((s,i)=>s+i.gross,0);const catMap={};items.forEach(i=>{if(!catMap[i.category])catMap[i.category]={name:i.category,gross:0,net:0,profit:0,qty:0,count:0,untracked:0,products:[]};const c=catMap[i.category];c.gross+=i.gross;c.net+=i.net;c.profit+=i.grossProfit||0;c.qty+=i.qty;c.count++;if(!i.hasCost)c.untracked++;c.products.push(i)});const categories=Object.values(catMap).map(c=>({...c,margin:c.net>0?(c.profit/c.net)*100:0,pctRev:totalGross>0?(c.gross/totalGross)*100:0,products:c.products.sort((a,b)=>b.gross-a.gross)})).sort((a,b)=>b.gross-a.gross);const prevItems=allDays.length>1?allDays[allDays.length-2]?.items||[]:[];const prevMap={};prevItems.forEach(i=>{prevMap[i.barcode]=i});const trending=items.filter(i=>{const prev=prevMap[i.barcode];return i.qty>=3&&prev&&prev.qty>0&&((i.qty-prev.qty)/prev.qty)>=.4&&i.hasCost&&(i.grossProfit||0)>.5}).map(i=>{const prev=prevMap[i.barcode];return{...i,prevQty:prev.qty,trendPct:Math.round(((i.qty-prev.qty)/prev.qty)*100)}}).sort((a,b)=>b.trendPct-a.trendPct).slice(0,15);const review=tracked.filter(i=>i.grossMargin!=null&&i.grossMargin<10&&i.grossMargin>=0&&i.qty>=1).sort((a,b)=>(a.grossMargin||0)-(b.grossMargin||0)).slice(0,15);const erosion=tracked.filter(i=>i.grossMargin!=null&&i.grossMargin<5).sort((a,b)=>(a.grossMargin||0)-(b.grossMargin||0)).slice(0,15);const catTopBottom={};categories.forEach(cat=>{const wc=cat.products.filter(p=>p.hasCost);catTopBottom[cat.name]={top:[...wc].sort((a,b)=>(b.grossProfit||0)-(a.grossProfit||0)).slice(0,5),bottom:[...wc].sort((a,b)=>(a.grossProfit||0)-(b.grossProfit||0)).slice(0,5)}});return{summary:{totalGross,totalNet,totalQty,productCount:items.length,trackedProfit,trackedMargin,trackedCount:tracked.length,untrackedCount:untracked.length,untrackedRevenue:untrackedRev,estimatedHidden:untrackedRev*(trackedMargin/100||.25),categoryCount:categories.length},categories,trending,review,erosion,catTopBottom,items,tracked,untracked,prevItems}}

function DashboardSection({analysis,dates,allDays,timeRange}){const{summary,categories}=analysis;const dateLabel=dates?(dates.start===dates.end?new Date(dates.start+"T12:00:00").toLocaleDateString("en-GB",{weekday:"short",day:"numeric",month:"short"}):`${new Date(dates.start+"T12:00:00").toLocaleDateString("en-GB",{day:"numeric",month:"short"})} — ${new Date(dates.end+"T12:00:00").toLocaleDateString("en-GB",{day:"numeric",month:"short"})}`):"";const chartData=allDays.map(d=>({label:d.dates?new Date(d.dates.start+"T12:00:00").toLocaleDateString("en-GB",{day:"numeric",month:"short"}):"?",revenue:d.items.reduce((s,i)=>s+i.gross,0),profit:d.items.filter(i=>i.hasCost).reduce((s,i)=>s+(i.grossProfit||0),0)}));const prevDay=allDays.length>1?allDays[allDays.length-2]:null;const prevGross=prevDay?prevDay.items.reduce((s,i)=>s+i.gross,0):null;const prevProfit=prevDay?prevDay.items.filter(i=>i.hasCost).reduce((s,i)=>s+(i.grossProfit||0),0):null;const catData=categories.slice(0,8).map(c=>({name:c.name.length>12?c.name.slice(0,12)+"…":c.name,gross:c.gross}));return(<SectionCard title="Dashboard" icon="📊" accent="rgba(46,80,144,0.15)"><div style={{fontSize:11,color:C.textMuted,marginBottom:12}}>{dateLabel} · {timeRange}</div><div style={{display:"flex",flexWrap:"wrap",gap:8,marginBottom:16}}><Stat label="Revenue" value={fi(summary.totalGross)} sub={prevGross?`${dt(summary.totalGross,prevGross)>0?"+":""}${dt(summary.totalGross,prevGross)}% vs prev`:null} trend={prevGross?(summary.totalGross>prevGross?1:-1):0}/><Stat label="Gross Profit" value={fi(summary.trackedProfit)} sub={prevProfit?`${dt(summary.trackedProfit,prevProfit)>0?"+":""}${dt(summary.trackedProfit,prevProfit)}%`:`${summary.trackedCount} tracked`} trend={prevProfit?(summary.trackedProfit>prevProfit?1:-1):0}/><Stat label="Avg Margin" value={pct(summary.trackedMargin)}/></div><div style={{display:"flex",flexWrap:"wrap",gap:8,marginBottom:16}}><Stat label="Items Sold" value={summary.totalQty.toLocaleString()} small/><Stat label="Products" value={summary.productCount.toLocaleString()} small/><Stat label="Hidden Profit" value={fi(summary.estimatedHidden)} sub={`${summary.untrackedCount} items`} trend={-1} small/></div>{chartData.length>1&&<><div style={{fontSize:11,fontWeight:700,color:C.textMuted,textTransform:"uppercase",letterSpacing:.8,marginBottom:8}}>Revenue Trend</div><div style={{height:160,marginBottom:16}}><ResponsiveContainer width="100%" height="100%"><AreaChart data={chartData} margin={{top:4,right:4,bottom:0,left:-10}}><CartesianGrid stroke={C.border} strokeDasharray="3 3" vertical={false}/><XAxis dataKey="label" tick={{fill:C.textMuted,fontSize:9}} axisLine={false} tickLine={false}/><YAxis tick={{fill:C.textMuted,fontSize:9}} axisLine={false} tickLine={false} tickFormatter={v=>`£${v}`}/><Tooltip content={<ChartTip/>}/><Area type="monotone" dataKey="revenue" stroke={C.accentLight} fill="rgba(59,111,212,0.15)" name="Revenue"/><Area type="monotone" dataKey="profit" stroke={C.green} fill="rgba(34,197,94,0.1)" name="Profit"/></AreaChart></ResponsiveContainer></div></>}<div style={{fontSize:11,fontWeight:700,color:C.textMuted,textTransform:"uppercase",letterSpacing:.8,marginBottom:8}}>Revenue by Category</div><div style={{height:200}}><ResponsiveContainer width="100%" height="100%"><BarChart data={catData} layout="vertical" margin={{top:4,right:4,bottom:0,left:-10}}><CartesianGrid stroke={C.border} strokeDasharray="3 3" horizontal={false}/><XAxis type="number" tick={{fill:C.textMuted,fontSize:9}} axisLine={false} tickLine={false} tickFormatter={v=>`£${v}`}/><YAxis type="category" dataKey="name" tick={{fill:C.textMuted,fontSize:9}} axisLine={false} tickLine={false} width={90}/><Tooltip content={<ChartTip/>}/><Bar dataKey="gross" fill={C.accentLight} radius={[0,4,4,0]} name="Revenue"/></BarChart></ResponsiveContainer></div></SectionCard>)}

function TrendingSection({analysis}){const{trending}=analysis;if(!trending.length)return<SectionCard title="Trending Up" icon="📈"><EmptyState msg="Upload multiple days to see trending products (40%+ increase vs previous period)"/></SectionCard>;return(<SectionCard title="Trending Up — 40%+ vs Previous" icon="📈" noPad><div style={{padding:"8px 16px",fontSize:11,color:C.textSecondary}}>Selling 40%+ more. Min 3 units & positive profit.</div><TableRow header cells={[{v:"Product",flex:2.5},{v:"Qty"},{v:"Prev"},{v:"Trend"},{v:"Profit"}]}/>{trending.map((p,i)=><TableRow key={i} cells={[{v:p.product,flex:2.5,color:C.white,bold:true},{v:p.qty},{v:p.prevQty,color:C.textMuted},{v:`+${p.trendPct}%`,color:C.greenText,bold:true},{v:f(p.grossProfit||0),color:C.greenText}]}/>)}<div style={{padding:"10px 16px",fontSize:10,color:C.textMuted}}>💡 Ensure stock matches rising demand.</div></SectionCard>)}

function ReviewSection({analysis}){const{review}=analysis;if(!review.length)return<SectionCard title="Review" icon="⚠️"><EmptyState msg="No low-margin items detected"/></SectionCard>;return(<SectionCard title="Review — Low Margin Items" icon="⚠️" noPad><div style={{padding:"8px 16px",fontSize:11,color:C.textSecondary}}>Items with margin below 10%. Consider repricing.</div><TableRow header cells={[{v:"Product",flex:2.5},{v:"Qty"},{v:"Margin"},{v:"Revenue"}]}/>{review.map((p,i)=><TableRow key={i} cells={[{v:p.product,flex:2.5,color:C.white},{v:p.qty},{v:pct(p.grossMargin||0),color:p.grossMargin<5?C.redText:C.orangeText,bold:true},{v:f(p.gross)}]}/>)}</SectionCard>)}

function ErosionSection({analysis}){const{erosion}=analysis;if(!erosion.length)return<SectionCard title="Margin Erosion" icon="🚨"><EmptyState msg="No critical margin items"/></SectionCard>;const neg=erosion.filter(i=>(i.grossMargin||0)<0);return(<SectionCard title="Margin Erosion Alert" icon="🚨" noPad><div style={{padding:"8px 16px",fontSize:11,color:C.textSecondary}}>Items below 5% margin. Negative = selling at a loss.</div>{neg.length>0&&<div style={{margin:"0 16px 8px",padding:10,borderRadius:10,background:C.redDim,border:"1px solid rgba(239,68,68,0.2)"}}><div style={{fontSize:10,fontWeight:700,color:C.redText}}>⚠️ {neg.length} items at a LOSS</div><div style={{fontSize:11,color:C.textPrimary,marginTop:4}}>Check cost entries — may be data errors.</div></div>}<TableRow header cells={[{v:"Product",flex:2.5},{v:"Margin"},{v:"Qty"},{v:"Revenue"}]}/>{erosion.map((p,i)=><TableRow key={i} cells={[{v:p.product,flex:2.5,color:C.white},{v:pct(p.grossMargin||0),color:(p.grossMargin||0)<0?C.redText:C.orangeText,bold:true},{v:p.qty},{v:f(p.gross)}]}/>)}</SectionCard>)}

function CategoriesSection({analysis}){const[expanded,setExpanded]=useState(null);const{categories,catTopBottom}=analysis;return(<SectionCard title="Categories" icon="📦" noPad><TableRow header cells={[{v:"Category",flex:2},{v:"Revenue"},{v:"Profit"},{v:"Margin"},{v:"%"}]}/>{categories.map((cat,idx)=><div key={cat.name}><div onClick={()=>setExpanded(expanded===idx?null:idx)} style={{cursor:"pointer"}}><TableRow cells={[{v:cat.name,flex:2,color:C.white,bold:true},{v:fi(cat.gross),bold:true},{v:cat.profit>0?fi(cat.profit):"—",color:cat.profit>0?C.greenText:C.textMuted},{v:cat.margin>0?pct(cat.margin):"—",color:cat.margin>=25?C.greenText:cat.margin<10&&cat.margin>0?C.redText:C.textPrimary},{v:pct(cat.pctRev)}]}/></div>{expanded===idx&&catTopBottom[cat.name]&&<div style={{padding:"8px 16px 12px",background:C.surface}}>{cat.untracked>0&&<div style={{marginBottom:8}}><Badge type="UNTRACKED">{cat.untracked} no cost data</Badge></div>}<div style={{fontSize:10,fontWeight:700,color:C.greenText,marginBottom:4,textTransform:"uppercase",letterSpacing:.5}}>▲ Top 5</div>{catTopBottom[cat.name].top.map((p,i)=><div key={i} style={{display:"flex",justifyContent:"space-between",padding:"3px 0",fontSize:10}}><span style={{color:C.textPrimary,flex:2,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{p.product}</span><span style={{color:C.textMuted,minWidth:35,textAlign:"right"}}>×{p.qty}</span><span style={{color:C.greenText,fontWeight:600,minWidth:50,textAlign:"right"}}>{f(p.grossProfit||0)}</span></div>)}<div style={{fontSize:10,fontWeight:700,color:C.redText,marginTop:10,marginBottom:4,textTransform:"uppercase",letterSpacing:.5}}>▼ Bottom 5</div>{catTopBottom[cat.name].bottom.map((p,i)=><div key={i} style={{display:"flex",justifyContent:"space-between",padding:"3px 0",fontSize:10}}><span style={{color:C.textPrimary,flex:2,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{p.product}</span><span style={{color:C.textMuted,minWidth:35,textAlign:"right"}}>×{p.qty}</span><span style={{color:(p.grossProfit||0)<0?C.redText:C.textMuted,fontWeight:600,minWidth:50,textAlign:"right"}}>{p.hasCost?f(p.grossProfit||0):"—"}</span></div>)}</div>}</div>)}</SectionCard>)}

function TopSellersSection({analysis}){const[sortBy,setSortBy]=useState("profit");const topItems=useMemo(()=>{const t=analysis.tracked;if(sortBy==="profit")return[...t].sort((a,b)=>(b.grossProfit||0)-(a.grossProfit||0)).slice(0,20);if(sortBy==="revenue")return[...analysis.items].sort((a,b)=>b.gross-a.gross).slice(0,20);return[...t].sort((a,b)=>(b.grossMargin||0)-(a.grossMargin||0)).slice(0,20)},[analysis,sortBy]);return(<SectionCard title="Top Sellers" icon="💰" noPad><div style={{padding:"8px 16px",display:"flex",gap:4}}>{["profit","revenue","margin"].map(s=><button key={s} onClick={()=>setSortBy(s)} style={{flex:1,padding:"6px 0",borderRadius:8,border:"none",fontSize:10,fontWeight:700,cursor:"pointer",background:sortBy===s?C.accentLight:C.surface,color:sortBy===s?C.white:C.textMuted}}>{s==="profit"?"By Profit":s==="revenue"?"By Revenue":"By Margin"}</button>)}</div><TableRow header cells={[{v:"Product",flex:2.5},{v:"Qty"},{v:sortBy==="margin"?"Margin":sortBy==="revenue"?"Revenue":"Profit"},{v:"Cat"}]}/>{topItems.map((item,i)=><TableRow key={i} cells={[{v:`${i+1}. ${item.product}`,flex:2.5,color:i<3?C.white:C.textPrimary,bold:i<3},{v:item.qty},{v:sortBy==="margin"?pct(item.grossMargin||0):sortBy==="revenue"?f(item.gross):f(item.grossProfit||0),color:C.greenText,bold:true},{v:item.category.length>10?item.category.slice(0,10)+"…":item.category}]}/>)}</SectionCard>)}

function MissingDataSection({analysis}){const{untracked,summary}=analysis;const avgM=summary.trackedMargin/100;const catBreak=useMemo(()=>{const cats={};untracked.forEach(i=>{if(!cats[i.category])cats[i.category]={name:i.category,count:0,revenue:0};cats[i.category].count++;cats[i.category].revenue+=i.gross});return Object.values(cats).sort((a,b)=>b.revenue-a.revenue)},[untracked]);return(<SectionCard title="Hidden Profit" icon="🔍" accent="rgba(239,68,68,0.06)"><div style={{display:"flex",flexWrap:"wrap",gap:8,marginBottom:16}}><Stat label="Untracked Rev" value={fi(summary.untrackedRevenue)} sub={`${pct(summary.untrackedRevenue/summary.totalGross*100)} of total`} trend={-1}/><Stat label="Est. Hidden GP" value={fi(summary.estimatedHidden)} sub={`@ ${pct(summary.trackedMargin)}`} trend={-1}/><Stat label="Items" value={untracked.length}/></div><div style={{background:`linear-gradient(135deg,rgba(46,80,144,0.12),rgba(59,130,246,0.06))`,borderRadius:10,padding:12,border:`1px solid rgba(46,80,144,0.2)`,marginBottom:16}}><div style={{fontSize:10,color:C.accentLight,fontWeight:700,textTransform:"uppercase",letterSpacing:1,marginBottom:4}}>Quick Win</div><div style={{fontSize:12,color:C.textPrimary,lineHeight:1.5}}>Enter costs for top 5 items to recover <strong style={{color:C.greenText}}>{fi(untracked.slice(0,5).reduce((s,i)=>s+i.gross,0)*avgM)}</strong> in visibility.</div></div>{catBreak.map((cat,i)=><div key={i} style={{display:"flex",justifyContent:"space-between",padding:"6px 0",borderBottom:`1px solid ${C.divider}`}}><span style={{fontSize:11,color:C.textPrimary}}>{cat.name}</span><span style={{fontSize:11,color:C.textMuted}}>{cat.count}</span><span style={{fontSize:11,color:C.white,fontWeight:600}}>{fi(cat.revenue)}</span></div>)}<div style={{fontSize:11,fontWeight:700,color:C.textMuted,textTransform:"uppercase",letterSpacing:.8,marginTop:16,marginBottom:8}}>Top Untracked</div>{untracked.slice(0,15).map((item,i)=><div key={i} style={{display:"flex",justifyContent:"space-between",padding:"6px 0",borderBottom:`1px solid ${C.divider}`}}><div style={{flex:2}}><div style={{fontSize:11,color:C.white,fontWeight:i<3?600:400}}>{item.product}</div><div style={{fontSize:9,color:C.textMuted}}>{item.category} · ×{item.qty}</div></div><div style={{textAlign:"right"}}><div style={{fontSize:11,color:C.orangeText,fontWeight:600}}>{f(item.gross)}</div><div style={{fontSize:9,color:C.textMuted}}>~{f(item.gross*avgM)} hidden</div></div></div>)}</SectionCard>)}

function OpsSection({analysis,allDays}){if(allDays.length<2)return<SectionCard title="Operations" icon="⚙️"><EmptyState msg="Upload multiple days to see patterns"/></SectionCard>;const dailyData=allDays.map(d=>{const gross=d.items.reduce((s,i)=>s+i.gross,0);const qty=d.items.reduce((s,i)=>s+i.qty,0);const day=d.dates?new Date(d.dates.start+"T12:00:00"):null;return{dayName:day?day.toLocaleDateString("en-GB",{weekday:"short"}):"?",gross,qty}});const busiest=[...dailyData].sort((a,b)=>b.gross-a.gross)[0];const quietest=[...dailyData].sort((a,b)=>a.gross-b.gross)[0];const avgD=dailyData.reduce((s,d)=>s+d.gross,0)/dailyData.length;const chartData=dailyData.map(d=>({label:d.dayName,revenue:d.gross,fill:d.gross>avgD*1.15?C.green:d.gross<avgD*.85?C.red:C.accentLight}));return(<SectionCard title="Operational Intelligence" icon="⚙️"><div style={{display:"flex",flexWrap:"wrap",gap:8,marginBottom:16}}><Stat label="Avg Daily" value={fi(avgD)} small/><Stat label="Busiest" value={busiest.dayName} sub={fi(busiest.gross)} small/><Stat label="Quietest" value={quietest.dayName} sub={fi(quietest.gross)} small/></div><div style={{height:160,marginBottom:12}}><ResponsiveContainer width="100%" height="100%"><BarChart data={chartData} margin={{top:4,right:4,bottom:0,left:-10}}><CartesianGrid stroke={C.border} strokeDasharray="3 3" vertical={false}/><XAxis dataKey="label" tick={{fill:C.textMuted,fontSize:10}} axisLine={false} tickLine={false}/><YAxis tick={{fill:C.textMuted,fontSize:9}} axisLine={false} tickLine={false} tickFormatter={v=>`£${v}`}/><Tooltip content={<ChartTip/>}/><Bar dataKey="revenue" name="Revenue" radius={[4,4,0,0]}>{chartData.map((d,i)=><Cell key={i} fill={d.fill}/>)}</Bar></BarChart></ResponsiveContainer></div><div style={{display:"flex",gap:8}}>{[{label:"Peak",color:C.green},{label:"Normal",color:C.accentLight},{label:"Quiet",color:C.red}].map(l=><div key={l.label} style={{display:"flex",alignItems:"center",gap:4,fontSize:9,color:C.textMuted}}><div style={{width:8,height:8,borderRadius:2,background:l.color}}/>{l.label}</div>)}</div></SectionCard>)}

function ComingUpSection(){const[events,setEvents]=useState(null);const[loading,setLoading]=useState(false);useEffect(()=>{let c=false;(async()=>{setLoading(true);try{const res=await fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:AI_HDR,body:JSON.stringify({model:"claude-sonnet-4-20250514",max_tokens:1000,messages:[{role:"user",content:`Today is ${new Date().toLocaleDateString("en-GB")}. List 6 upcoming events for a UK convenience store in NE England. Include holidays, sporting, school, payday, seasonal. JSON array only: {event,date,days,impact,priority}. Priority: URGENT/PLAN/AWARE. No markdown.`}]})});const data=await res.json();const text=data.content?.filter(b=>b.type==="text").map(b=>b.text).join("")||"";if(!c)setEvents(JSON.parse(text.replace(/```json|```/g,"").trim()))}catch{if(!c)setEvents([])}if(!c)setLoading(false)})();return()=>{c=true}},[]);return(<SectionCard title="Coming Up" icon="📅">{loading&&<div style={{textAlign:"center",padding:20,color:C.textMuted,fontSize:12}}>Loading...</div>}{events?.length===0&&<EmptyState msg="Could not load events"/>}{events?.map((e,i)=><div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"10px 12px",marginBottom:6,borderRadius:10,background:C.surface,border:`1px solid ${C.border}`}}><div style={{flex:1}}><div style={{fontSize:11,color:C.white,fontWeight:600}}>{e.event}</div><div style={{fontSize:10,color:C.textMuted}}>{e.date} · {e.days} · {e.impact}</div></div><Badge type={e.priority==="URGENT"?"ALERT":e.priority==="PLAN"?"MED":"OK"}>{e.priority}</Badge></div>)}</SectionCard>)}

function NewsSection(){const[news,setNews]=useState(null);const[loading,setLoading]=useState(false);useEffect(()=>{let c=false;(async()=>{setLoading(true);try{const res=await fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:AI_HDR,body:JSON.stringify({model:"claude-sonnet-4-20250514",max_tokens:2000,messages:[{role:"user",content:"Find 5 recent UK convenience store retail news. JSON array: {title,source,summary,url,timeAgo}. No markdown."}],tools:[{type:"web_search_20250305",name:"web_search"}]})});const data=await res.json();const text=data.content?.filter(b=>b.type==="text").map(b=>b.text).join("")||"";if(!c)setNews(JSON.parse(text.replace(/```json|```/g,"").trim()).slice(0,5))}catch{if(!c)setNews([])}if(!c)setLoading(false)})();return()=>{c=true}},[]);return(<div style={{paddingTop:4}}><div style={{fontSize:15,fontWeight:800,color:C.white,marginBottom:16}}>Retail News</div>{loading&&<div style={{textAlign:"center",padding:32,color:C.textMuted}}>Loading...</div>}{news?.map((item,i)=><a key={i} href={item.url} target="_blank" rel="noopener noreferrer" style={{textDecoration:"none",display:"block"}}><div style={{padding:"14px 16px",marginBottom:8,borderRadius:12,background:C.card,border:`1px solid ${C.border}`}}><div style={{display:"flex",justifyContent:"space-between",marginBottom:6}}><span style={{fontSize:10,fontWeight:700,color:C.accentLight,textTransform:"uppercase"}}>{item.source}</span><span style={{fontSize:10,color:C.textMuted}}>{item.timeAgo}</span></div><div style={{fontSize:13,fontWeight:700,color:C.white,lineHeight:1.4,marginBottom:4}}>{item.title}</div>{item.summary&&<div style={{fontSize:11,color:C.textSecondary,lineHeight:1.4}}>{item.summary}</div>}</div></a>)}</div>)}

function AIChatSection({analysis,allDays}){const[messages,setMessages]=useState([]);const[input,setInput]=useState("");const[loading,setLoading]=useState(false);const chatRef=useRef();const systemPrompt=useMemo(()=>{const{summary,categories}=analysis;const catS=categories.slice(0,15).map(c=>`${c.name}: £${c.gross.toFixed(2)} rev, £${c.profit.toFixed(2)} profit, ${c.qty} units, ${c.margin.toFixed(1)}%`).join("\n");const top=[...analysis.items].sort((a,b)=>b.gross-a.gross).slice(0,40).map(i=>`${i.product}(${i.category}):qty=${i.qty},gross=£${i.gross.toFixed(2)},profit=${i.hasCost?"£"+(i.grossProfit||0).toFixed(2):"UNTRACKED"},margin=${i.hasCost?(i.grossMargin||0)+"%":"N/A"}`).join("\n");return`You are an AI retail analyst for a UK convenience store. ${allDays.length} day(s) of data.\nRevenue £${summary.totalGross.toFixed(2)}, Profit £${summary.trackedProfit.toFixed(2)}, Margin ${summary.trackedMargin.toFixed(1)}%, ${summary.productCount} products, ${summary.untrackedCount} untracked (£${summary.untrackedRevenue.toFixed(2)}).\nCATEGORIES:\n${catS}\nTOP 40:\n${top}\nBe concise and actionable.`},[analysis,allDays]);const send=useCallback(async()=>{if(!input.trim()||loading)return;const msg=input.trim();setInput("");setMessages(p=>[...p,{role:"user",content:msg}]);setLoading(true);try{if(!ANTHROPIC_KEY){setMessages(p=>[...p,{role:"assistant",content:"API key not configured. Add your Anthropic API key to config.js to enable AI features."}]);setLoading(false);return;}const h=[...messages,{role:"user",content:msg}];const res=await fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:AI_HDR,body:JSON.stringify({model:"claude-sonnet-4-20250514",max_tokens:1000,system:systemPrompt,messages:h})});const data=await res.json();if(data.error){setMessages(p=>[...p,{role:"assistant",content:`API error: ${data.error.message||JSON.stringify(data.error)}`}]);setLoading(false);return;}const text=data.content?.filter(b=>b.type==="text").map(b=>b.text).join("")||"No response.";setMessages(p=>[...p,{role:"assistant",content:text}])}catch(e){setMessages(p=>[...p,{role:"assistant",content:`Connection error: ${e.message}`}])}setLoading(false);setTimeout(()=>chatRef.current?.scrollTo({top:chatRef.current.scrollHeight,behavior:"smooth"}),100)},[input,loading,messages,systemPrompt]);const suggestions=["Most profitable category?","Which items need costs entered?","How long will 10 cases of Fosters last?","What's driving revenue?"];return(<SectionCard title="AI Assistant" icon="🤖" accent="rgba(59,130,246,0.08)"><div ref={chatRef} style={{maxHeight:350,overflowY:"auto",marginBottom:12}}>{messages.length===0&&<div><div style={{fontSize:11,color:C.textSecondary,marginBottom:12}}>Ask about your sales data.</div><div style={{display:"flex",flexWrap:"wrap",gap:6}}>{suggestions.map((s,i)=><button key={i} onClick={()=>setInput(s)} style={{padding:"6px 10px",borderRadius:8,border:`1px solid ${C.border}`,background:C.surface,color:C.textSecondary,fontSize:10,cursor:"pointer"}}>{s}</button>)}</div></div>}{messages.map((m,i)=><div key={i} style={{marginBottom:10,display:"flex",justifyContent:m.role==="user"?"flex-end":"flex-start"}}><div style={{maxWidth:"85%",padding:"10px 14px",borderRadius:12,fontSize:12,lineHeight:1.6,background:m.role==="user"?C.accentLight:C.surface,color:m.role==="user"?C.white:C.textPrimary,border:m.role==="user"?"none":`1px solid ${C.border}`,whiteSpace:"pre-wrap"}}>{m.content}</div></div>)}{loading&&<div style={{display:"flex",gap:4,padding:"8px 14px"}}>{[0,1,2].map(i=><div key={i} style={{width:6,height:6,borderRadius:3,background:C.accentLight,opacity:.5,animation:`pulse 1s ease-in-out ${i*.2}s infinite`}}/>)}</div>}</div><div style={{display:"flex",gap:8}}><input value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder="Ask about your sales..." style={{flex:1,padding:"10px 14px",borderRadius:10,background:C.surface,color:C.white,border:`1px solid ${C.border}`,fontSize:12,outline:"none",fontFamily:"'Inter',sans-serif"}}/><button onClick={send} disabled={loading||!input.trim()} style={{padding:"10px 16px",borderRadius:10,border:"none",cursor:"pointer",background:input.trim()?C.accentLight:C.surface,color:input.trim()?C.white:C.textMuted,fontSize:12,fontWeight:700}}>Send</button></div></SectionCard>)}

function SearchView({analysis}){const[query,setQuery]=useState("");const[selected,setSelected]=useState(null);const results=useMemo(()=>{if(!query.trim())return[];const q=query.toLowerCase();return analysis.items.filter(i=>i.product.toLowerCase().includes(q)||i.category.toLowerCase().includes(q)||i.barcode.includes(q)).sort((a,b)=>b.gross-a.gross).slice(0,50)},[query,analysis.items]);const cats=useMemo(()=>{const m={};analysis.items.forEach(i=>{if(!m[i.category])m[i.category]={name:i.category,count:0,revenue:0};m[i.category].count++;m[i.category].revenue+=i.gross});return Object.values(m).sort((a,b)=>b.revenue-a.revenue)},[analysis.items]);return(<div style={{paddingTop:4}}><div style={{position:"relative",marginBottom:14}}><div style={{position:"absolute",left:14,top:"50%",transform:"translateY(-50%)",fontSize:16,color:C.textMuted,pointerEvents:"none"}}>🔍</div><input type="text" value={query} onChange={e=>{setQuery(e.target.value);setSelected(null)}} placeholder="Search products..." style={{width:"100%",padding:"12px 14px 12px 42px",borderRadius:12,background:C.card,color:C.white,border:`1px solid ${C.border}`,fontSize:14,fontWeight:500,outline:"none",fontFamily:"'Inter',sans-serif",boxSizing:"border-box"}}/>{query&&<button onClick={()=>{setQuery("");setSelected(null)}} style={{position:"absolute",right:12,top:"50%",transform:"translateY(-50%)",background:"none",border:"none",color:C.textMuted,fontSize:16,cursor:"pointer"}}>✕</button>}</div>{!query.trim()&&<div>{cats.map((cat,i)=><button key={i} onClick={()=>setQuery(cat.name)} style={{display:"flex",justifyContent:"space-between",alignItems:"center",width:"100%",padding:"11px 14px",marginBottom:5,background:C.card,border:`1px solid ${C.border}`,borderRadius:10,cursor:"pointer",textAlign:"left"}}><div><div style={{fontSize:12,fontWeight:600,color:C.white}}>{cat.name}</div><div style={{fontSize:10,color:C.textMuted}}>{cat.count} products · {fi(cat.revenue)}</div></div><div style={{fontSize:12,color:C.textMuted}}>›</div></button>)}</div>}{query.trim()&&results.length>0&&results.map((item,i)=><div key={i} onClick={()=>setSelected(selected===i?null:i)} style={{cursor:"pointer"}}><div style={{display:"flex",justifyContent:"space-between",padding:"10px 12px",marginBottom:4,borderRadius:10,background:selected===i?C.surface:C.card,border:`1px solid ${selected===i?C.accentLight:C.border}`}}><div style={{flex:2}}><div style={{fontSize:11,color:C.white,fontWeight:600}}>{item.product}</div><div style={{fontSize:9,color:C.textMuted}}>{item.category}</div></div><div style={{textAlign:"right"}}><div style={{fontSize:11,color:C.white,fontWeight:600}}>{f(item.gross)}</div><div style={{fontSize:9,color:item.hasCost?C.greenText:C.textMuted}}>×{item.qty} · {item.hasCost?pct(item.grossMargin):"no cost"}</div></div></div>{selected===i&&<div style={{padding:"8px 12px 12px",marginBottom:4,borderRadius:"0 0 10px 10px",background:C.surface,border:`1px solid ${C.border}`,borderTop:"none",display:"flex",flexWrap:"wrap",gap:8}}>{[["Qty",item.qty],["Gross",f(item.gross)],["Net",f(item.net)],["Profit",item.hasCost?f(item.grossProfit):"—"],["Margin",item.hasCost?pct(item.grossMargin):"—"]].map(([l,v])=><div key={l} style={{minWidth:60,textAlign:"center"}}><div style={{fontSize:9,color:C.textMuted,textTransform:"uppercase"}}>{l}</div><div style={{fontSize:12,fontWeight:700,color:C.white,marginTop:2}}>{v}</div></div>)}</div>}</div>)}{query.trim()&&results.length===0&&<EmptyState msg={`No results for "${query}"`}/>}</div>)}

function UploadScreen({onDataLoaded,uploads}){const[dragging,setDragging]=useState(false);const[error,setError]=useState(null);const[loading,setLoading]=useState(false);const fileRef=useRef();const handleFile=useCallback(async(file)=>{if(!file)return;setError(null);setLoading(true);try{const buf=await file.arrayBuffer();const data=parseFile(buf,file.name);onDataLoaded(data,file.name)}catch(e){setError(e.message||"Failed")}setLoading(false)},[onDataLoaded]);return(<div style={{padding:"40px 20px",display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"center",minHeight:"60vh"}}><div style={{width:64,height:64,borderRadius:16,background:`linear-gradient(135deg,${C.accentLight},${C.green})`,display:"flex",alignItems:"center",justifyContent:"center",fontSize:28,marginBottom:20}}>📊</div><div style={{fontSize:20,fontWeight:800,color:C.white,marginBottom:6}}>ShopMate Sales</div><div style={{fontSize:12,color:C.textMuted,marginBottom:32,textAlign:"center"}}>Upload your daily Item Sales Report</div><div onDragOver={e=>{e.preventDefault();setDragging(true)}} onDragLeave={()=>setDragging(false)} onDrop={e=>{e.preventDefault();setDragging(false);handleFile(e.dataTransfer.files[0])}} onClick={()=>fileRef.current?.click()} style={{width:"100%",maxWidth:360,padding:"40px 20px",borderRadius:16,cursor:"pointer",border:`2px dashed ${dragging?C.accentLight:C.border}`,textAlign:"center",background:dragging?C.accentGlow:C.card}}>{loading?<div style={{fontSize:14,color:C.accentLight,fontWeight:600}}>Processing...</div>:<><div style={{fontSize:36,marginBottom:12}}>📁</div><div style={{fontSize:13,color:C.white,fontWeight:600,marginBottom:6}}>Tap to select file</div><div style={{fontSize:11,color:C.textMuted}}>or drag & drop .xls export</div></>}</div><input ref={fileRef} type="file" accept=".xls,.xlsx" multiple style={{display:"none"}} onChange={e=>Array.from(e.target.files).forEach(handleFile)}/>{error&&<div style={{marginTop:16,padding:"10px 16px",borderRadius:10,background:C.redDim,border:"1px solid rgba(239,68,68,0.3)",fontSize:12,color:C.redText,maxWidth:360}}>{error}</div>}{uploads.length>0&&<div style={{marginTop:24,width:"100%",maxWidth:360}}><div style={{fontSize:11,fontWeight:700,color:C.textMuted,textTransform:"uppercase",letterSpacing:.8,marginBottom:8}}>Uploaded ({uploads.length})</div>{uploads.map((u,i)=><div key={i} style={{display:"flex",justifyContent:"space-between",padding:"8px 12px",marginBottom:4,borderRadius:8,background:C.card,border:`1px solid ${C.border}`}}><span style={{fontSize:11,color:C.white}}>{u.dates?.start||"?"}</span><span style={{fontSize:11,color:C.textMuted}}>{u.items.length} products · {fi(u.items.reduce((s,i)=>s+i.gross,0))}</span></div>)}</div>}<div style={{marginTop:16,fontSize:10,color:C.textMuted,textAlign:"center"}}>Upload multiple days for trending & operations</div></div>)}

const sectionList=[{id:"dashboard",label:"Dashboard",icon:"📊"},{id:"cats",label:"Categories",icon:"📦"},{id:"trending",label:"Trending",icon:"📈"},{id:"review",label:"Review",icon:"⚠️"},{id:"topsellers",label:"Top Sellers",icon:"💰"},{id:"erosion",label:"Erosion",icon:"🚨"},{id:"missing",label:"Hidden Profit",icon:"🔍"},{id:"ops",label:"Operations",icon:"⚙️"},{id:"coming",label:"Coming Up",icon:"📅"},{id:"ai",label:"AI",icon:"🤖"}];
const sectionGrid=[{id:"dashboard",icon:"📊",label:"Dashboard",sub:"KPIs & overview"},{id:"cats",icon:"📦",label:"Categories",sub:"Top & bottom per category"},{id:"trending",icon:"📈",label:"Trending",sub:"40%+ vs previous"},{id:"review",icon:"⚠️",label:"Review",sub:"Low margin items"},{id:"topsellers",icon:"💰",label:"Top Sellers",sub:"Best profit contributors"},{id:"erosion",icon:"🚨",label:"Erosion",sub:"Margin alerts"},{id:"missing",icon:"🔍",label:"Hidden Profit",sub:"Missing cost data"},{id:"ops",icon:"⚙️",label:"Operations",sub:"Daily patterns"},{id:"coming",icon:"📅",label:"Coming Up",sub:"Events & prep"},{id:"ai",icon:"🤖",label:"AI Assistant",sub:"Ask about your data"}];
const bottomNav=[{id:"home",icon:"🏠",label:"Home"},{id:"search",icon:"🔍",label:"Search"},{id:"grid",icon:"⊞",label:"Sections"},{id:"news",icon:"📰",label:"News"}];
const timeRanges=[{id:"day",label:"Day"},{id:"week",label:"Week"},{id:"month",label:"Month"}];

export default function ShopMateDashboard(){const[allDays,setAllDays]=useState([]);const[activeSection,setActiveSection]=useState("dashboard");const[activeTab,setActiveTab]=useState("home");const[timeRange,setTimeRange]=useState("day");const[clientId,setClientId]=useState(null);const[clientName,setClientName]=useState("");const[sbStatus,setSbStatus]=useState("");const[loading,setLoading]=useState(true);

// On mount: get owner from URL, load from Supabase
useEffect(()=>{(async()=>{const ownerId=getOwnerIdFromURL();if(!ownerId){setLoading(false);return;}
try{const client=await getOrCreateClient(ownerId);
if(!client){setSbStatus("Client error");setLoading(false);return;}
setClientId(client.id);setClientName(client.name||ownerId);
console.log("Client ID:",client.id);
const days=await loadFromSupabase(client.id);if(days.length>0)setAllDays(days);setSbStatus(days.length>0?`${days.length} days loaded`:"Ready");}catch(e){console.error("Init:",e);setSbStatus("Error: "+e.message);}
setLoading(false);})();},[]);

const addDay=useCallback(async(data)=>{
  // Add to local state
  setAllDays(prev=>{if(prev.find(d=>d.dates?.start===data.dates?.start))return prev.map(d=>d.dates?.start===data.dates?.start?data:d);return[...prev,data].sort((a,b)=>(a.dates?.start||"").localeCompare(b.dates?.start||""))});
  // Push to Supabase if we have a client
  if(clientId){setSbStatus("Saving...");const result=await pushToSupabase(clientId,data);setSbStatus(result.ok?`✓ Saved ${result.rows} rows`:`✗ ${result.error}`);setTimeout(()=>setSbStatus(""),3000);}
},[clientId]);const currentData=useMemo(()=>{if(!allDays.length)return null;if(timeRange==="day")return allDays[allDays.length-1];return{items:allDays.flatMap(d=>d.items),dates:{start:allDays[0].dates?.start,end:allDays[allDays.length-1].dates?.end}}},[allDays,timeRange]);const analysis=useMemo(()=>currentData?analyzeData(allDays,currentData):null,[currentData,allDays]);const hour=new Date().getHours();const greeting=hour<12?"Good morning":hour<18?"Good afternoon":"Good evening";

if(loading)return(<div style={{background:C.bg,minHeight:"100vh",maxWidth:480,margin:"0 auto",fontFamily:"'Inter','SF Pro Display',-apple-system,sans-serif",color:C.textPrimary,display:"flex",alignItems:"center",justifyContent:"center"}}><div style={{textAlign:"center"}}><div style={{fontSize:24,marginBottom:12}}>📊</div><div style={{fontSize:14,color:C.white,fontWeight:600}}>Loading...</div><div style={{fontSize:11,color:C.textMuted,marginTop:4}}>Connecting to Supabase</div></div><style>{globalCSS}</style></div>);

if(!analysis)return(<div style={{background:C.bg,minHeight:"100vh",maxWidth:480,margin:"0 auto",fontFamily:"'Inter','SF Pro Display',-apple-system,sans-serif",color:C.textPrimary}}>{!clientId&&<div style={{padding:"12px 20px",background:C.orangeDim,borderBottom:`1px solid rgba(245,158,11,0.2)`}}><div style={{fontSize:11,color:C.orangeText,fontWeight:600}}>⚠️ No owner ID in URL — data won't save to Supabase</div><div style={{fontSize:10,color:C.textMuted,marginTop:4}}>Add ?owner=YOUR_ID to the URL for persistence</div></div>}<UploadScreen onDataLoaded={addDay} uploads={allDays}/><style>{globalCSS}</style></div>);

const{summary}=analysis;const dateLabel=currentData.dates?(timeRange==="day"?new Date(currentData.dates.start+"T12:00:00").toLocaleDateString("en-GB",{day:"numeric",month:"short"}):`${allDays.length} days`):"";const rangeLabel=timeRange==="day"?"Today":timeRange==="week"?"This Week":"This Month";

const renderSection=()=>{switch(activeSection){case"dashboard":return<DashboardSection analysis={analysis} dates={currentData.dates} allDays={allDays} timeRange={rangeLabel}/>;case"cats":return<CategoriesSection analysis={analysis}/>;case"trending":return<TrendingSection analysis={analysis}/>;case"review":return<ReviewSection analysis={analysis}/>;case"topsellers":return<TopSellersSection analysis={analysis}/>;case"erosion":return<ErosionSection analysis={analysis}/>;case"missing":return<MissingDataSection analysis={analysis}/>;case"ops":return<OpsSection analysis={analysis} allDays={allDays}/>;case"coming":return<ComingUpSection/>;case"ai":return<AIChatSection analysis={analysis} allDays={allDays}/>;default:return<DashboardSection analysis={analysis} dates={currentData.dates} allDays={allDays} timeRange={rangeLabel}/>}};

return(<div style={{background:C.bg,minHeight:"100vh",maxWidth:480,margin:"0 auto",fontFamily:"'Inter','SF Pro Display',-apple-system,sans-serif",color:C.textPrimary,position:"relative",overflow:"hidden"}}><div style={{position:"fixed",top:-150,right:-100,width:400,height:400,background:"radial-gradient(circle,rgba(46,80,144,0.15) 0%,transparent 70%)",pointerEvents:"none",zIndex:0}}/><div style={{padding:"20px 20px 12px",position:"relative",zIndex:1,background:"linear-gradient(180deg,rgba(46,80,144,0.08) 0%,transparent 100%)"}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:16}}><div><div style={{display:"flex",alignItems:"center",gap:8,marginBottom:6}}><div style={{width:32,height:32,borderRadius:8,background:`linear-gradient(135deg,${C.accentLight},${C.green})`,display:"flex",alignItems:"center",justifyContent:"center",fontSize:16,fontWeight:900,color:C.white}}>S</div><span style={{fontSize:15,fontWeight:800,color:C.white,letterSpacing:.5}}>ShopMate Sales</span></div><div style={{fontSize:13,color:C.textSecondary}}>{greeting}{clientName?`, ${clientName}`:""}</div><div style={{fontSize:11,color:C.textMuted,marginTop:2}}>{dateLabel} · {allDays.length} day{allDays.length!==1?"s":""}{sbStatus?` · ${sbStatus}`:""}</div></div><div style={{display:"flex",flexDirection:"column",gap:6,alignItems:"flex-end"}}><div style={{padding:"4px 10px",borderRadius:20,fontSize:10,fontWeight:700,background:C.greenDim,color:C.greenText,border:"1px solid rgba(34,197,94,0.2)"}}>● LIVE</div><button onClick={()=>setActiveTab("upload")} style={{padding:"4px 10px",borderRadius:8,background:C.surface,border:`1px solid ${C.border}`,color:C.textMuted,fontSize:10,fontWeight:600,cursor:"pointer"}}>+ Upload</button></div></div><div style={{display:"flex",gap:4,marginBottom:12}}>{timeRanges.map(tr=><button key={tr.id} onClick={()=>setTimeRange(tr.id)} style={{flex:1,padding:"6px 0",borderRadius:8,border:"none",fontSize:11,fontWeight:700,cursor:"pointer",background:timeRange===tr.id?C.accentLight:C.surface,color:timeRange===tr.id?C.white:C.textMuted}}>{tr.label}</button>)}</div><div style={{display:"flex",gap:8,padding:"12px 14px",borderRadius:12,background:`linear-gradient(135deg,${C.card},rgba(46,80,144,0.1))`,border:`1px solid ${C.border}`,boxShadow:"0 4px 16px rgba(0,0,0,0.3)"}}><Stat label="Revenue" value={fi(summary.totalGross)} small/><div style={{width:1,background:C.border}}/><Stat label="Profit" value={fi(summary.trackedProfit)} small/><div style={{width:1,background:C.border}}/><Stat label="Margin" value={pct(summary.trackedMargin)} small/></div></div>{activeTab==="home"&&<div style={{display:"flex",gap:6,padding:"12px 20px",overflowX:"auto",WebkitOverflowScrolling:"touch",scrollbarWidth:"none",position:"relative",zIndex:1}}>{sectionList.map(s=><button key={s.id} onClick={()=>setActiveSection(s.id)} style={{padding:"6px 14px",borderRadius:20,border:"none",cursor:"pointer",fontSize:11,fontWeight:600,whiteSpace:"nowrap",transition:"all 0.2s",background:activeSection===s.id?C.accentLight:C.card,color:activeSection===s.id?C.white:C.textMuted,boxShadow:activeSection===s.id?"0 2px 12px rgba(59,111,212,0.3)":"none"}}>{s.icon} {s.label}</button>)}</div>}<div style={{padding:"8px 16px 100px",position:"relative",zIndex:1}}>{activeTab==="home"&&renderSection()}{activeTab==="search"&&<SearchView analysis={analysis}/>}{activeTab==="grid"&&<div style={{padding:"8px 0"}}><div style={{fontSize:15,fontWeight:800,color:C.white,marginBottom:16,paddingLeft:4}}>Sections</div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}>{sectionGrid.map(s=><button key={s.id} onClick={()=>{setActiveSection(s.id);setActiveTab("home")}} style={{background:C.card,border:`1px solid ${C.border}`,borderRadius:14,padding:"16px 14px",textAlign:"left",cursor:"pointer"}}><span style={{fontSize:24,display:"block",marginBottom:8}}>{s.icon}</span><div style={{fontSize:13,fontWeight:700,color:C.white,marginBottom:4}}>{s.label}</div><div style={{fontSize:10,color:C.textMuted,lineHeight:1.3}}>{s.sub}</div></button>)}</div></div>}{activeTab==="news"&&<NewsSection/>}{activeTab==="upload"&&<UploadScreen onDataLoaded={addDay} uploads={allDays}/>}</div><div style={{position:"fixed",bottom:0,left:"50%",transform:"translateX(-50%)",width:"100%",maxWidth:480,zIndex:10,background:`linear-gradient(180deg,transparent,${C.bg} 20%)`,padding:"20px 16px 12px"}}><div style={{display:"flex",justifyContent:"space-around",alignItems:"center",padding:"10px 0",borderRadius:20,background:C.card,border:`1px solid ${C.border}`,boxShadow:"0 -4px 24px rgba(0,0,0,0.4)"}}>{bottomNav.map(n=><button key={n.id} onClick={()=>{setActiveTab(n.id);if(n.id==="home")setActiveSection("dashboard")}} style={{background:"none",border:"none",cursor:"pointer",display:"flex",flexDirection:"column",alignItems:"center",gap:2,opacity:activeTab===n.id?1:.5,transition:"opacity 0.2s"}}><span style={{fontSize:n.id==="grid"?22:20}}>{n.icon}</span><span style={{fontSize:9,fontWeight:700,letterSpacing:.3,color:activeTab===n.id?C.accentLight:C.textMuted}}>{n.label}</span></button>)}</div></div><style>{globalCSS}</style></div>)}

const globalCSS=`*::-webkit-scrollbar{display:none}*{scrollbar-width:none;box-sizing:border-box}@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800;900&display=swap');@keyframes fadeIn{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:translateY(0)}}@keyframes pulse{0%,100%{opacity:.3}50%{opacity:1}}`;
